# Aluxion Labs Challenge Node.js

## Instalacion
Para ejecutar el proyecto en su computadora, clone el repositorio en su carpeta de trabajo.

Abra un terminal y ejecute el siguiente comando:

```sh
npm install
```

En la raíz del directorio, crea un nuevo archivo llamado .env y agregue las variables de entorno que se encuentran en el archivo .env.sample

finalmente, ejecute el siguiente comando:

```sh
npm start
```
